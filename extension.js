const vscode = require('vscode');

function activate(context) {
    console.log('YaarScript extension is now active!');
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
};
